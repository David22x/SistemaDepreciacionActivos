$kind: collection
name: "Categorías"
description: 'Consulta de categorías de activos. Requiere JWT Bearer token.'
order: 3000
auth:
  type: bearer
  credentials:
    - key: token
      value: '{{token}}'
