$kind: http-request
order: 5000
method: DELETE
url: '{{baseUrl}}/assets/{{activoID}}'
scripts:
  - type: afterResponse
    language: text/javascript
    code: |-
      pm.test("Activo eliminado (204)", function () {
          pm.response.to.have.status(204);
      });
